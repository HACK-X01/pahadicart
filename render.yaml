services:
  - type: web
    name: pahadicart-himachal
    env: node
    buildCommand: "echo 'PahadiCart ready for Himalayan deployment'"
    startCommand: "node server.js"
    envVars:
      - key: PORT
        value: 3333
      - key: NODE_ENV
        value: production
